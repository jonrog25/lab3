<!-- Basic skeleton generated by Emmet Abbreviation "!" in VS Code -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grocery Helper</title>

    <!-- Materialize CSS and Icons-->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">

    <!-- Additional custom CSS -->
    <link rel="stylesheet" href="/stylesheets/assignmentsStyle.css">

</head>

<body>

    <!-- Nav bar -->
    <header>
        <nav>
            <div class="nav-wrapper">
                <a href="/" class="brand-logo"><i class="material-icons left">school</i>Grocery Helper</a>
                <a href="#" data-target="mobile-nav" class="sidenav-trigger"><i class="material-icons">menu</i></a>
                <ul id="desktop-nav" class="right hide-on-med-and-down">
                    <li><a href="/"><i class="material-icons left">home</i> Home</a></li>
                    <li><a href="/assignments"><i class="material-icons left">list</i> Food List</a></li>                </ul>
            </div>
        </nav>
        <!-- <ul id="mobile-nav" class="sidenav">
            <li><a href="/"><i class="material-icons left">home</i> Home</a></li>
            <li><a href="assignments"><i class="material-icons left">list</i> Assignments</a></li>
        </ul> -->
    </header>
    
    <!-- Actual page content! -->
    <div class="container">
        <div class="section flow-text" id="details">
            <h1><%= food.food_name %></h1>
            <table>
                <tr>
                    <th>Food Name:</th>
                    <td><%= food.food_name %></td>
                </tr>
                <tr>
                    <th>Amount:</th>
                    <td><%= food.food_amt%></td>
                </tr>
                <tr>
                    <th>Store:</th>
                    <td><%= food.store_name%></td>
                </tr>
                <tr>
                    <th>Description</th>
                     <td>
                        <%= food.food_desc%>
                    </td>
                    
                </tr>
            </table>
        </div>

        <div class="section" id="controls">
            <a class="btn-large waves-effect waves-light orange left modal-trigger" href="#edit">
                <i class="material-icons right">edit</i>
                Edit
            </a>
            <a class="btn-large waves-effect waves-light red right" href = "/assignments/<%= food.food_id %>">
                <i class="material-icons right">delete</i>
                Delete
            </a>
        </div>


        <div class="modal" id="edit">

            <div class="modal-content">

                <h2>Edit item:</h2>
                <form id="updateForm" method = "post" action = "/assignments/<%= food.food_id %>" >
                    <div class="row">
                        <div class="input-field col s8">
                            <input type="text" id="foodInput" name="food_name" class="validate" data-length="45" required>
                            <label for="foodInput">Food Name</label>
                        </div>
                        <div class="input-field col s8">
                            <input type="text" id="foodDesct" name="food_desc" class="validate" data-length="45" required>
                            <label for="foodDesct">Food Description</label>
                        </div>
                        <div class="input-field col s4">
                            <input type="number" id="foodAmt" name="food_amt" class="validate" min=0 max=100 required>
                            <label for="foodAmt">Amount</label>
    
                        </div>
    
                         <div class="input-field col s6">
                            <select tpye = "number" id="store_name" name="store" required>
                                <option value="" disabled selected>Choose your store</option>
                                <option value=1   <% if (food.food_id == 1) { %> selected <% } %>> Trader Joes </option>
                                <option value=2   <% if (food.food_id == 2) { %> selected <% } %>> Shoprite </option>
                                <option value=3   <% if (food.food_id == 3) { %> selected <% } %>> Krogers </option>
                                <option value=4   <% if (food.food_id == 4) { %> selected <% } %>> Stop and Shop </option>
                                <option value=5   <% if (food.food_id == 5) { %> selected <% } %>> Wegmans </option>
                            </select>
                            <label for="store_name">Store:</label>
                        </div>
                       
    
                    
                    </div>
                    <button class="btn waves-effect waves-light" type="submit">
                        <i class="material-icons right">send</i>
                        Submit
                    </button>
                </form>
                <!-- A section for the footer of the modal -->
                <div class="modal-footer">
                    <!-- Since the submit button is not within the form, it must have the form attribute specified-->
                    <button class="btn waves-effect waves-light " type="submit" form="updateForm">
                        <i class="material-icons right">send</i>
                        Submit
                    </button>
                    <button class="modal-close waves-effect waves-green btn-flat" >Never Mind</button>
                </div>
                
            </div>

        </div>
    </div>

    <!-- Materialize JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>

    <!-- Auto-initialize Materialize elements (Note - more specific initialization may be required for certain elements)-->
    <script src="/scripts/materializeAutoInit.js"></script>

</body>

</html>